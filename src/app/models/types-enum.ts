export namespace TypesEnum {
  export enum Types {
    //here add further types if needed i.e. textarea or anything else you need and then just add some code to your template for new type of field
    INPUT = 'input',
    RADIO = 'radio',
    SELECT = 'select'
  }
}
